---
type Props = {
  icon: string;
  class?: string;
};

const { icon, ...rest } = Astro.props as Props;
const iconFile = await import(`../icons/${icon}.svg`);
---

<i {...rest}>
  <img src={iconFile.default.src} width={48} />
</i>
